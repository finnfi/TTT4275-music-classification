

class KNN_classifier:
    def __init__(self,training_set, features, k):
        '''
        training_set: variable of TrainingSet
        features: array of feature strings
        k: int 
        '''
        self.k = k
        self.dim = len(features)
        self.num_points = 0
        self.training_set = training_set
        for cls in traning_set.__dict__.values():
            num_points += len(cls)
        self.points = np.zeros(num_points,dim)
        self.index = array(*SongFeatures)
        i = 0
        for cls in traning_set.__dict__.values():
            for song in cls:
                for feature in features:
                    
    
    def normalise():
        var = np.variance(self.points(:,1))

    def classify(x):
        
        pass